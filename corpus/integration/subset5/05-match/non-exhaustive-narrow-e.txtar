-- stdout --
-- stderr --
error[SEMANTIC_ERROR]: incompatible type: expected int, got int|string
  --> non-exhaustive-narrow-e.bal:25:13
   |
25 |     int y = x; // @error match is not exhaustive so x is still int|string here
   |             ^
